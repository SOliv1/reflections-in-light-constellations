:{$PORT:8080} {
	@apiRoutes {
		path /api/* /days* /upload* /random-image* /health
	}
	handle @apiRoutes {
		reverse_proxy {$API_UPSTREAM:http://reflections-in-light-constellations.railway.internal:5000}
	}

	handle {
		root * /usr/share/caddy
		try_files {path} /index.html
		file_server
	}
}
